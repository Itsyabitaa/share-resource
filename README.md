# Markdown Share App

Create or upload Markdown files and share them via link.

## How to Run

1. Clone the repo
2. Run `npm install`
3. Run `npm run dev` to start locally
4. Deploy to Vercel

Powered by Next.js + Supabase.